/// App-side runtime for the `sqflite_devtools` DevTools extension.
///
/// See `SqfliteDevTools.register`. The wire format used between the app and
/// the extension lives in `package:sqflite_devtools/protocol.dart`.
library;

export 'src/service.dart' show SqfliteDevTools;
