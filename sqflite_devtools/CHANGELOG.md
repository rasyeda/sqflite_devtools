## 0.1.1

- Stretch the data grid to fill the pane instead of floating it centred when the
  table is narrower than the available width.
- Add a screenshot of the extension to the README.

## 0.1.0

- Initial release: table and view browser, schema inspector, paged data grid and
  SQL editor, exposed through `ext.sqflite_devtools.*` service extensions.
